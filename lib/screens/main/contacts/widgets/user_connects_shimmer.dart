import 'package:flutter/material.dart';

class UserConnectsShimmer extends StatelessWidget {
  const UserConnectsShimmer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 10,
    );
  }
}
