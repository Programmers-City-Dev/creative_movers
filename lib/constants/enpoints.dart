import 'package:creative_movers/helpers/api_helper.dart';

class Endpoints {
  static const login_endpoint =
      '${ApiConstants.BASE_URL}api/login';

      static const register_endpoint =
      '${ApiConstants.BASE_URL}api/register';
}